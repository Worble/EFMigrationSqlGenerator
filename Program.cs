using System;

namespace MigrationGenerator
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("This project is just a scaffold for Entity Framework, and should not be run directly. Please run the 'generate_sql.py' script at the root of the project.");
            Environment.Exit(0);
        }
    }
}
